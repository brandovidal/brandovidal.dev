---
interface Props {
  class?: string;
  id?: string;
}
const { class: className, id } = Astro.props;
---

<section id={id} class=`w-full mx-auto lg:w-[748px] pb-24 ${className}`>
  <slot />
</section>
