---
interface Props {
  class?: string;
  id?: string;
}
const { class: className, id } = Astro.props;
---

<section id={id} class=`w-full mx-auto lg:w-[900px] px-4 sm:px-6 lg:px-8 py-20 sm:py-24 md:py-32 ${className}`>
  <slot />
</section>
