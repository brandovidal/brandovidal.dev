---
interface Props {
  class?: string;
  id?: string;
}
const { class: className, id } = Astro.props;
---

<section id={id} class=`w-full mx-auto lg:w-[748px] py-12 sm:py-16 md:py-24 ${className}`>
  <slot />
</section>
